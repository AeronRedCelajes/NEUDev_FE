server {
    listen 80;
    server_name _;  # or your domain if you have one

    # Serve your built frontend from this directory
    root /usr/share/nginx/html;
    index index.html;

    # This block makes unknown routes fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}